<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head') %>
    <link rel="stylesheet" href="/css/userProfile.css">
    <title>Perfil de usuario</title>
</head>

<body>
    <%- include('./partials/navbar') %>
    <main>
        <section>

            <div class="user-image-name">
                <img src="img/profiles/<%= locals.session.imagen %> " alt="">
                <h2> <%= locals.session.user %></h2>
                <h3>Total publicaciones: <%= publicaciones.length %> </h3>
            </div>

            <div class="user-info">
                <div>
                    <p class="info-title">Nombre:</p>
                    <p class="info-data"><%= usuario.nombre %></p>
                </div>
                <div>
                    <p class="info-title">Apellido:</p>
                    <p class="info-data"><%= usuario.apellido %></p>
                </div>
                <div>
                    <p class="info-title">Telefono:</p>
                    <p class="info-data"><%= usuario.telefono %></p>
                </div>
                <div>
                    <p class="info-title">Fecha de nacimiento:</p>
                    <p class="info-data"><%= usuario.fechanac %></p>
                </div>
                <div>
                    <p class="info-title">Email:</p>
                    <p class="info-data"><%= usuario.email %></p>
                </div>
            </div>
        </section>
    </main>
    <%- include('./partials/footer') %>
    <%- include('./partials/sidenav') %>
</body>

</html>