<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/login.css">
    <%- include('./partials/head') %>
    <title>Login</title>
</head>
<body>
    <%- include('./partials/navbar') %>
    <div class="loginbox">
        <form action="/login" method="post">
            <% if(locals.mensaje && locals.mensaje.msg){ %>
                <br>
                <div class="error"><%= locals.mensaje.msg %></div>
            <% } %>
            <div class="loginuser">
                <label for="user">Nombre de usuario o Email:</label>
                <input 
                    data-mensaje="Nombre de usuario o Email"
                    type="text" 
                    name="user" 
                    id="user" 
                    onchange="validation(this, 2, 30)"
                    value="<%= locals.old && old.user ? old.user : "" %>">
                <% if(locals.errors && locals.errors.user){ %>
                    <div class="error"><%= locals.errors.user.msg %></div>
                <% } %>
               
            </div>
            <div class="loginpassword">
                <label for="password">Contraseña:</label>
                <input 
                    data-mensaje="Contraseña"
                    type="password" 
                    name="password" 
                    id="password" 
                    onchange="validation(this, 6, 30)">
                <% if(locals.errors && locals.errors.password){ %>
                    <div class="error"><%= locals.errors.password.msg %></div>
                <% } %>
            </div>

<label class="content-input">
	<input type="checkbox" name="recordar" id="recordar">Recordar
	<i></i>
</label>
            <div class="recordar">
                <label for="recordar"></label>
                <input type="checkbox" name="recordar" id="recordar">Recordarme en el equipo
            </div>
            <div class="loginbutton">
                <button type="submit">Iniciar sesion</button>
                <a href="#" class="olvido">Olvidaste tu contraseña?</a>
            </div>
            <div class="registrate">
                    <span>No tienes una cuenta?</span> 
                    <a href="/registro">Registrate</a>       
            </div>
            
        </form>
    </div>
</body>
    <script src="/js/loginValidation.js"></script>
</html>